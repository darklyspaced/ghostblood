[unstable]
# enable the unstable artifact-dependencies feature, see
# https://doc.rust-lang.org/nightly/cargo/reference/unstable.html#artifact-dependencies
bindeps = true

# [alias]
# soulcaster = "run --package soulcaster --target aarch64-apple-darwin --"
# sc = "run --package soulcaster --target aarch64-apple-darwin --"
